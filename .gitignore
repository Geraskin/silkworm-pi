# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Local app data (lives on the Pi only, never committed)
latest.jpg
latest.dng
camweb.h264
settings.json
timelapse/

# Frames copied down for rendering with scripts/stabilize-timelapse.sh. A single
# session is a few hundred 8 MP JPEGs, which has no business in the repository.
tmp/

# Machine-specific deploy target (Pi address, key path)
.github/skills/deploy-pi/deploy.env

# Local dev environment (built per machine, not part of the repo)
.devcontainer/

# Editors / OS
.vscode/
.DS_Store
